Build the flashcards app for production without deploying.

Steps:
1. Run `npm run build` (runs `tsc -b && vite build`)
2. Report the build output size and any warnings
3. If TypeScript errors occur, list them clearly so they can be fixed
